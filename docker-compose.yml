services:
  springboot-api:
    build: ./
    environment:
      - SPRING_DATA_MONGODB_URI=${SPRING_DATA_MONGODB_URI:-mongodb+srv://dilshansachintha322_db_user:yIVjtiko5I2prZxv@fxexchange.ewzv1pj.mongodb.net/currency_db?appName=fxExchange}
    ports:
      - "8084:8084"
    expose:
      - "8084"

  node-client:
    build: ./UI
    environment:
      - API_BASE_URL=http://springboot-api:8084
    ports:
      - "3000:3000"
    depends_on:
      - springboot-api